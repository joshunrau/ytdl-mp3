import { describe, expect, it } from 'vitest';

import { SongTagsSearch } from '../SongTagsSearch';

describe('SongTagsSearch', () => {
  it('should be defined', () => {
    expect(SongTagsSearch).toBeDefined();
  });
});
